name: "lint and test"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    types: [ opened, synchronize, reopened ]

jobs:
  lint-and-test:
    uses: esclient/tools/.github/workflows/lint-api-gateway-python.yml@main
    with:
      python-version: '3.10.4'
