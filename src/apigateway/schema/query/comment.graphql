type CommentQuery {
    getComments(input: GetCommentsInput!): [Comment]
}

type Comment {
    id: ID!
    author_id: ID!
    text: String!
    created_at: DateTime!
    edited_at: DateTime
}

input GetCommentsInput {
    mod_id: ID!
}

